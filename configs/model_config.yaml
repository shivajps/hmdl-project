# IoT-SHIELD architecture config — placeholders; set to your actual final values
input_dim: 100          # pre-CLF-GWO feature count; selected count ~26.6 avg (Table 6)
num_classes: 18
lstm_hidden: 128
lstm_layers: 2
gcn_hidden: 64
gcn_layers: 2
fused_dim: 128
dropout: 0.3
focal_gamma: 2.0
learning_rate: 1.0e-3
batch_size: 256
epochs: 50
seq_len: 50              # tau, manuscript Section 6.3.1